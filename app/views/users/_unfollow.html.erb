<!--use form_for to manipulate a Relationship model object
find an existing relationship.
sends a DELETE request to destroy a relationship. So we need the ID to send (use hidden for that purpose)-->
<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
            remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn" %>
<% end %>